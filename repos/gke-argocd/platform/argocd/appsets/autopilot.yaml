apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: tenants-autopilot
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - list:
        elements:
          - tenant: hanica
          - tenant: oke
  template:
    metadata:
      name: '{{ .tenant }}'
      labels:
        smarthr.io/tenant: '{{ .tenant }}'
        smarthr.io/cluster-flavor: autopilot
    spec:
      project: '{{ .tenant }}'
      source:
        repoURL: https://github.com/tayzar-tznw/gke-argocd.git
        targetRevision: main
        # In real life (separate gke-argocd repo) this is just
        # `tenants/{{ .tenant }}/overlays/autopilot`. The `repos/gke-argocd/`
        # prefix exists only because this demo nests the four real-life repos
        # under repos/ to show them side-by-side. See docs/MULTI_REPO_LAYOUT.md.
        path: 'repos/gke-argocd/tenants/{{ .tenant }}/overlays/autopilot'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{ .tenant }}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=false
          - ServerSideApply=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
