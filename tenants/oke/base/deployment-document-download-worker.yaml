apiVersion: apps/v1
kind: Deployment
metadata:
  name: document-download-worker
  namespace: oke
  labels:
    app.kubernetes.io/name: document-download-worker
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: oke
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: document-download-worker
      app.kubernetes.io/part-of: oke
  template:
    metadata:
      labels:
        app.kubernetes.io/name: document-download-worker
        app.kubernetes.io/component: worker
        app.kubernetes.io/part-of: oke
    spec:
      serviceAccountName: worker
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: worker
          image: redis:7-alpine
          command: ["/bin/sh", "-c"]
          args:
            - |
              QUEUE=document-download-queue
              while true; do
                ITEM=$(redis-cli -h redis.oke.svc.cluster.local LPOP $QUEUE)
                if [ -n "$ITEM" ]; then
                  echo "[doc-download] streaming $ITEM"
                  sleep 2
                else
                  sleep 2
                fi
              done
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: 500m, memory: 512Mi}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
